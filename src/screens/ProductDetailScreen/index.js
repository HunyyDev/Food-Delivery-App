import {StyleSheet, Text, View, SafeAreaView} from 'react-native';
import { IC_Heart, IC_Back} from '../../assets/icons';
import React from 'react';

const ProductDetail = () => {
  return (
    <SafeAreaView>
      <Text>index</Text>
    </SafeAreaView>
  );
};

export default ProductDetail;

const styles = StyleSheet.create({});
